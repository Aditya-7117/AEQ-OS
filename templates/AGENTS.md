# Agent instructions for this project

Before any work: read `~/.ai_os/BOOT.md` and execute its boot protocol.

AEQ-OS classifies this project against `~/.ai_os/ROUTER_MAP.json` and loads the matching rule files (constitution, model-adaptation, stack selection, verification gates, plus the domain files this project's signals match — quant execution, live trading, AI agents, RAG, full-stack, UI/UX, deployment). Cite rule IDs (e.g. `CONST-11`, `QT-STOP-3`, `LIVE-9`) in code review and commit messages.

If `~/.ai_os/` is not installed on this machine, see https://github.com/Aditya-7117/AEQ-OS for setup.
